language: node_js
node_js:
  - 14
install:
  - npm install -g zcatalyst-cli
jobs:
  include:
    - stage: functions
      before_script:
        - cd functions/crm_crud
        - pwd
      script:
        - echo 'functions!!'
        - echo 'Installing Dependencies!'
        - npm install
      after_success:
        - echo 'deploying functions!'
        - echo ${CATALYST_TOKEN}
        - catalyst --version
    - stage: client
      before_script:
        - cd client/app
        - pwd
      script:
        - echo 'client!!'
        - echo 'Installing Dependencies!'
        - yarn install
        - yarn build
        - cp ../client-package.json build/.
        - cd build
      after_success:
        - echo 'deploying client!'
        - catalyst deploy --only client --project 128000000004001 --verbose --token m_1004.96322ca20dda8ff03524cd5939791255.acc549c3d0c2fb0dd612e4d9d923655d